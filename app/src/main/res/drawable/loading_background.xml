<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Зовнішній прямокутник -->
    <item>
        <shape android:shape="rectangle">
            <solid
                android:color="@android:color/transparent"/>
            <stroke
                android:width="2dp"
                android:color="#262626" />
            <corners android:radius="8dp" />
        </shape>
    </item>

    <!-- Внутрішній прямокутник -->
    <item
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp">
        <shape android:shape="rectangle">
            <solid android:color ="@android:color/transparent"/> <!-- Колір внутрішнього прямокутника -->
            <stroke
                android:width="2dp"
                android:color="#191919" />
            <corners android:radius="5dp" />
        </shape>
    </item>

</layer-list>
